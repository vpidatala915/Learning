Factory pattern si pretty straight-forward

The idea being there is a parent class that determines
what methods that need be implemented,

Which is then inherited by the child classes
Note is only about the methods not the members
We do not care about the members